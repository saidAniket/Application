#include<stdio.h>
int CountDigit(int iNo)
{
    
    return 0;
}
int main()
{
    int iValue=0;
    int iret=0;

    printf("Enter the no\n");
    scanf("%d",&iValue);

    iret=CountDigit(iValue);
    printf("Number of digit are : %d\n",iret);
    return 0;
}